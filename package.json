{
  "name": "performancegym-aarhus-dk",
  "description": "Website for performancegymaarhus.dk",
  "private": true,
  "scripts": {
    "setup": "npm install && bundle install",
    "postinstall": "find node_modules/ -name '*.info' -type f -delete",
    "start": "jekyll server --port 8080 --incremental"
  },
  "dependencies": {
    "gulp": "~3.9.0"
  },
  "devDependencies": {
    "gulp-notify": "^2.2.0",
    "jshint": "~2.9.1",
    "gulp-jshint": "~2.0.0",
    "sass-lint": "^1.5.1",
    "gulp-sass-lint": "^1.1.1",
    "imagemin-pngcrush": "~4.1.0",
    "gulp-imagemin": "~2.2.1",
    "gulp-autoprefixer": "^3.1.0"
  }
}
